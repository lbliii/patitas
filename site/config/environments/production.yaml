# Production Environment
# =======================

site:
  # GitHub Pages project site: deployed at /patitas
  baseurl: "/patitas"

build:
  incremental: true
  strict_mode: true

assets:
  minify: true
  fingerprint: true
